<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="1dp" />
            <gradient
                android:startColor="@color/litegray"
                android:centerColor="@color/litegray"
                android:centerY="1.0"
                android:endColor="@color/litegray"
                android:angle="270"/>
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="1dp" />
                <gradient
                    android:startColor="@color/LiteGreen_transparenta"
                    android:centerColor="@color/LiteGreen_transparenta"
                    android:centerY="1.0"
                    android:endColor="@color/LiteGreen_transparenta"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>